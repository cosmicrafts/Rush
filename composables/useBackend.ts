// Backend unico de Rush: WOU-ID (identidad) + ledger Ionic-Swap (dinero)
// + nftropoly (cartas). Sin cadenas externas.
import { useLocalBackend as useWeb3 } from './useLocalBackend'

export { useWeb3 }
